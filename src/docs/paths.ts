import { categoryPath, specificationPath, categoryImportPath, sessionPath, carPath } from './paths/'

export default {
  '/categories': categoryPath,
  '/specifications': specificationPath,
  '/categories/import': categoryImportPath,
  '/sessions': sessionPath,
  '/cars': carPath
}